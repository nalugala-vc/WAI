# Weather-AI API key — required for local dev and Vercel deployment
VITE_WAI_API_KEY=wai_your_key_here

# Optional — only if production place search hits CORS errors (see README)
# VITE_NOMINATIM_BASE_URL=https://your-server.example/nominatim
